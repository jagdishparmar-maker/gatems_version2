# Public PocketBase API URL (no trailing slash). Defaults to production if unset.
NEXT_PUBLIC_POCKETBASE_URL=https://pocketbase.intoship.cloud

# Optional (dev only): extra hostnames allowed for /_next/* when using next dev from another machine (comma-separated).
# ALLOWED_DEV_ORIGINS=192.168.1.10,phone.local
